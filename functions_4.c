#include<stdio.h>
int sum(int a,int b);
     main()
{
	int a,b,r;
	printf("Enter a,b values");
	scanf("%d%d",&a,&b);
	r=sum(a,b);
	printf("Addition=%d\n",r);
}
int sum(int x,int y)
{
	return(a+b);
}
